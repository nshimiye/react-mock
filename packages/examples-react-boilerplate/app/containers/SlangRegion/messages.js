/*
 * SlangRegion Messages
 *
 * This contains all the text for the SlangRegion container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.SlangRegion';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SlangRegion container!',
  },
});
